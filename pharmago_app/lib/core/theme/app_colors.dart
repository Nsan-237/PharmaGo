import 'package:flutter/material.dart';

class AppColors {
  // Brand colors (Matching high-fidelity Figma/Android mockups)
  static const Color primaryDark = Color(0xFF0D3B36);   // Deep Forest Green
  static const Color primary = Color(0xFF006C59);       // Emerald Green #006C59
  static const Color primaryLight = Color(0xFFE8F5F2);  // Soft Mint Background #E8F5F2
  static const Color primaryAccent = Color(0xFF00A884); // Bright Accent Teal
  static const Color accent = Color(0xFFE8A33D);        // Warm Gold / Amber
  static const Color accentLight = Color(0xFFFEF3DC);   // Soft Amber

  // Background & Neutrals
  static const Color background = Color(0xFFF8FAF9);    // Clean Soft Off-White
  static const Color cardBg = Colors.white;
  static const Color border = Color(0xFFE2EBE8);        // Clean crisp border
  static const Color divider = Color(0xFFEDF2F0);
  
  // Text Colors
  static const Color textDark = Color(0xFF0A2925);      // Rich dark green-grey
  static const Color textBody = Color(0xFF4A5568);      // Slate text
  static const Color textMuted = Color(0xFF8C9B96);     // Muted green-grey
  
  // Status Colors
  static const Color success = Color(0xFF16A34A);
  static const Color successLight = Color(0xFFDCFCE7);
  static const Color warning = Color(0xFFD97706);
  static const Color warningLight = Color(0xFFFEF3C7);
  static const Color error = Color(0xFFDC2626);
  static const Color errorLight = Color(0xFFFEE2E2);
  static const Color info = Color(0xFF2563EB);
  static const Color infoLight = Color(0xFFDBEAFE);

  // Map accents
  static const Color mapBg = Color(0xFFEBF3F0);
  static const Color mapPin = Color(0xFF006C59);
  static const Color mapPinAccent = Color(0xFF2563EB);
}
